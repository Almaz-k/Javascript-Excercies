//what to do?creat car object
//how to do ?declare const object name car then define the proportoes and their values
const car={
    make:'Tesla',
    model:'Model Y',
    year:'2009',
star:function(){//add new method 
    console.log('This care is starting');
}
}
console.log(`car model:${car.model}`);
console.log(`car year:${car.year}`);
console.log(`car make:${car.make}`);
//car.start();// this is the method to invoke the function
